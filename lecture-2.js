// Condition False ==>    null, "", 0, undefined, NaN, false,

// Condition True ==>    '0', [], {}, 'false', non zero numbers, non empty strings, all objects, all arrays,


const age = 10;

if (age > 0) {
    console.log('Condition is true!!!');
} else {
    console.log('Condition is false!!!');
}